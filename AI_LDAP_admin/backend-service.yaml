apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: ldap
spec:
  type: NodePort
  selector:
    app: backend
  ports:
    - protocol: TCP
      port: 8000
      nodePort: 31006
      targetPort: 8000